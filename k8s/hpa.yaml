# HPA 비활성 (부하 측정 시 일관된 환경 위해 replicas 1 고정).
# 활성화 시 아래 주석 풀고 workflow 의 'Apply HPA' step 도 같이 활성화.
#
# apiVersion: autoscaling/v2
# kind: HorizontalPodAutoscaler
# metadata:
#   name: wallet-service
#   labels:
#     app: wallet-service
# spec:
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: wallet-service
#   minReplicas: 1
#   maxReplicas: 5
#   metrics:
#     - type: Resource
#       resource:
#         name: cpu
#         target:
#           type: Utilization
#           averageUtilization: 70
